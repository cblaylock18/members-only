<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/css/styles.css" />
        <title>Create a New Message</title>
    </head>
    <body>
        <h1>Create a new message to share with other users!</h1>
        <a class="home-link" href="/">Return to Home</a>
        <%- include("partials/errors.ejs") %>
        <form action="/messages/new-message" method="post">
            <label for="title">Title: </label>
            <input type="text" name="title" id="title" placeholder="title"
            value="<%= locals.title ? title : "" %>" required />
            <label for="text">New Message: </label>
            <textarea
                class="message-contents"
                name="text"
                id="text"
                placeholder="message content"
                required
                rows="5"
            >
<%= locals.text ? text : "" %></textarea
            >
            <button type="submit">Submit Message</button>
        </form>
    </body>
</html>
